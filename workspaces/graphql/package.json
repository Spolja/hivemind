{
  "name": "@hivemind/graphql",
  "version": "0.0.1",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "dev": "ts-node src/index.ts",
    "apollo-router": "bin/router --dev --supergraph schema.graphql"
  }
}
